const v2 = new Uint8Array(2);
v2.toSorted();
gc();
// Imported program is interesting due to new coverage: 90 newly discovered edges in the CFG of the target
// Imported program is interesting due to new coverage: 20 newly discovered edges in the CFG of the target
