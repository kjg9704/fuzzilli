const v2 = new Int8Array(170);
for (const v6 in { 1: undefined, __proto__: v2 }) {
    Math.sin(-4);
    const v10 = BigInt64Array || -4;
    -4 - BigInt64Array;
    v10 * -4;
}
gc();
// Imported program is interesting due to new coverage: 348 newly discovered edges in the CFG of the target
// Imported program is interesting due to new coverage: 141 newly discovered edges in the CFG of the target
